'use strict';

export default (theme) => {
  return {
    base: {
      fontSize: '1.2em',
      float: 'left',
      color: theme.palette.primary2Color
    },
    iconfield: {
      marginBottom: '-4px'
    }
  };
};
