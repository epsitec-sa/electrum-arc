{
  "name": "electrum-arc",
  "version": "2.0.0",
  "description": "Agnostic Reactive Components for Electrum",
  "main": "lib/index.js",
  "scripts": {
    "babel": "babel --quiet --out-dir lib src",
    "compile": "rimraf ./lib && require-self && npm run regen && npm run babel",
    "rebuild": "npm run regen && npm run babel",
    "prepublish": "npm run compile",
    "regen": "electrum-require-components --crlf --wrap ./src components .component.js all-components.js",
    "test": "npm run compile",
    "watch": "chokidar \"src/**/*.js\" -i \"src/all-components.js\" -c \"npm run rebuild\""
  },
  "author": "Epsitec SA",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/epsitec-sa/electrum-arc.git"
  },
  "bugs": {
    "url": "https://github.com/epsitec-sa/electrum-arc/issues"
  },
  "homepage": "https://github.com/epsitec-sa/electrum-arc",
  "dependencies": {
    "electrum": "^2.20.0",
    "electrum-utils": "^1.5.0",
    "leaflet": "^0.7.7",
    "velocity-animate": "^1.2.3"
  },
  "devDependencies": {
    "babel-env": "^1.0.0",
    "chokidar-cli": "^1.2.0",
    "mai-chai": "^2.0.1",
    "require-self": "^0.1.0",
    "rimraf": "^2.5.2"
  }
}
